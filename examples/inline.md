# So basically I'm Title
## main function
```rust
fn main() {
```
`main` is a function such that it is equal to itself... yes.


```rust
    println!("So Basically I'm Str");
}
```
`println` is a macro that prints to `stdout`.